<% @page_title = "Blog"%>

<%= render :partial => @posts %>

<%= will_paginate @posts%>

<br /><br />

	<% if session[:user_id] == "admin"%>
		<%= link_to 'Comments', :controller => :comments, :action => :index %>
	<%end%>